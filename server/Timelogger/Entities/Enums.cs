namespace Timelogger.Entities
{
    public enum ProjectStatus
    {
        Pending = 0,
        InProgress = 1,
        Completed = 2,
	}

    public enum TimeDuration
    {
        None = 0,
        ThirtyMinutes = 1,
        OneHour = 2,
        TwoHours = 3,
        ThreeHours = 4,
        FourHours = 5
    }
}
